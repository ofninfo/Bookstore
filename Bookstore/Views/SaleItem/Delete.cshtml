@model Bookstore.Models.SaleItemModel

@{
    ViewData["Title"] = "Excluir Item da Venda";
}

<h1>Excluir Item da Venda</h1>

<h3>Tem certeza de que deseja excluir cadastro deste item da venda?</h3>
<div>
    <h6>Ao clicar em excluir o registro de item da venda será removido da base de dados permanentemente.</h6>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Value)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Value)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Quantity)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Quantity)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Subtotal)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Subtotal)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="SaleItemId" />
        <input type="hidden" asp-for="SaleId" />
        <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> Excluir</button>
        <a asp-controller="Sale" asp-action="Edit" asp-route-id="@Model.SaleId" class="btn btn-outline-secondary"><i class="bi bi-reply"></i> Voltar</a>
    </form>
</div>
